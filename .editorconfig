# https://editorconfig.org
root = true

[*]
charset = utf-8
end_of_line = lf
insert_final_newline = true

# Markdown: preserve intentional trailing spaces (line breaks)
[*.{md,mdc}]
trim_trailing_whitespace = false
