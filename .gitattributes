# Normalize all detected text files to LF in the repository (WSL / Linux / CI).
# https://git-scm.com/docs/gitattributes
* text=auto eol=lf

# Shell scripts must use LF for WSL/Linux compatibility (same as default; kept explicit)
*.sh text eol=lf
